import React from 'react';

const NoResults = () => (
  <>
    <p>The entity resolution script did not return any results.</p>
    <p>
      To continue with export, please close this window and either select
      a previous resolution or uncheck &quot;entity resolution&quot;
    </p>
  </>
);

export default NoResults;
