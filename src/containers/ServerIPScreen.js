import React from 'react';

const ServerIPScreen = () => (
  <div className="screen">
    <div className="screen__heading">
      <h1 className="screen__heading-title">How can your Server be reached?</h1>
      <h2 className="screen__heading-subtitle">In order to contact this copy of Server when they are not on the same Local Area Network (LAN), your clients need to know which public IP address and port this computer can be reacted at.</h2>
    </div>
    <div className="grid__container grid--x-center">
      Public IP Address
    </div>
  </div>
);

export default ServerIPScreen;
